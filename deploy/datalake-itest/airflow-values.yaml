executor: LocalExecutor

defaultAirflowRepository: docker.m.daocloud.io/apache/airflow
defaultAirflowTag: "3.2.2"

nodeSelector:
  workload: cpu

postgresql:
  enabled: true
  image:
    registry: docker.m.daocloud.io
    repository: bitnamilegacy/postgresql
    tag: "16.1.0-debian-11-r15"
  primary:
    nodeSelector:
      workload: cpu
    persistence:
      enabled: true
      size: 20Gi

redis:
  enabled: false

statsd:
  enabled: false

scheduler:
  replicas: 2
  serviceAccount:
    create: true
    name: airflow-scheduler

apiServer:
  replicas: 2

triggerer:
  replicas: 1

dags:
  persistence:
    enabled: false
  gitSync:
    enabled: false

extraVolumes:
  - name: datalake-itest-dags
    configMap:
      name: datalake-itest-airflow-dags

extraVolumeMounts:
  - name: datalake-itest-dags
    mountPath: /opt/airflow/dags/repository
    readOnly: true

env:
  - name: _PIP_ADDITIONAL_REQUIREMENTS
    value: "apache-airflow-providers-cncf-kubernetes==10.20.0"

config:
  core:
    load_examples: "False"
  scheduler:
    standalone_dag_processor: "False"

createUserJob:
  defaultUser:
    enabled: false
