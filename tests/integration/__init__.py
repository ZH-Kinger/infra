"""Integration tests against a real lakeFS / CPFS / PAI dev environment.

These are skipped unless the required endpoints are provided via environment
variables, so `make test` stays runnable offline. See docs/runbook.md for the
environment contract.
"""
