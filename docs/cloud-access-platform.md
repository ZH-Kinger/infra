# 云账号自助平台

员工在一个地方完成：看自己有哪些云账号和权限、申请开账号、申请云账号权限、申请临时访问凭证、
查看云账号资产。管理员在另一套页面里处理名册、执行失败的申请和审计。面板和 `delivery` CLI
共用同一个后端，规则只写一份。

代码在 `src/delivery/`，部署见 [deploy/panel/README.md](../deploy/panel/README.md)。

## 1. 不能退让的规则

| # | 规则 | 落在哪里 |
|---|---|---|
| R1 | **任何开通、授权、发凭证，都必须有一条已通过的飞书审批。** 执行前后端实时查飞书审批实例，状态是 `APPROVED`、审批定义、发起人、表单里的申请单号都对得上才执行，并且至少有一位**不是申请人**的审批人点了通过（只有本人或自动通过的不算，approval.json 的 allow_self_approval 可放开）；不信回调内容、不信本地状态 | `approval.py` `verify_approved()`；`flows.py` 的 execute / claim 都先调它 |
| R2 | 员工只能给自己申请：权限申请的目标子账号必须是名册里已确认属于本人的号 | `flows.py` 提交时校验 |
| R3 | 申请只能从两处选：管理员预先定义的模板（用户组、角色、时长上限），或采集来的权限策略目录。策略目录由服务端规则把关：提权、身份管理、密钥管理、账单、审计类策略内置禁用（身份 / 账单 / 审计整个产品线只放只读），规则文件只能追加禁用；**自定义策略默认不开放**（名字看不出能做什么，执行身份自己的策略也是自定义策略），要逐条写进 `allow`；最长天数按风险定。用户不能提交目录外的策略、角色 ARN、用户组名 | `catalog.py`、`policies.py` |
| R4 | 访问凭证不落盘：临时凭证在员工领取时现签现发，只返回给本人，不写申请单、不写日志。初始密码只能在开通后 7 天内领取一次，且子账号必须是这张申请单新建、名册里没有对应给别人 | `flows.py` `claim_credential()` / `claim_password()` |
| R5 | 执行身份最小权限：只有开号、加组、移出组、扮演模板角色这几个动作，每次写云前核对凭证属于目标云账号。目前从面板服务器的环境变量读取；改用 ECS 实例角色是后续项 | `provision.py`、部署文档 |
| R6 | 员工身份只认 `feishu_union_id`（公司 IAM 规范） | 登录层（飞书 / oauth2-proxy） |
| R7 | 所有状态变化记审计：谁、何时、对哪张申请单、做了什么；审计只追加 | `tickets.py` 事件表 |

## 2. 角色

| 角色 | 能看到 | 能做 |
|---|---|---|
| 员工 | 我的云账号与权限、我的申请、我能领取的凭证、我所在云账号的资产概况 | 提交申请、撤回自己未审批的申请、领取已批准的凭证和初始密码 |
| 管理员 | 全员名册、全部申请、全部资产、审计 | 名册审核、重试执行失败的申请、关闭申请、维护模板 |

管理员名单只按 union_id（`identity/admins.json`）。**管理员自己提交的申请同样走飞书审批**，
管理员身份不能跳过 R1。

## 3. 申请类型

| 类型 | 员工填写 | 审批通过后执行 | 员工领取 |
|---|---|---|---|
| 开账号 `account` | 模板（哪个云账号、默认加入哪些组）、希望的用户名、用途 | 建子账号、加入模板里的用户组、写入人工对应（名册下次刷新生效） | 控制台初始密码（一次性，强制首次登录改密）|
| 云账号权限 `permission` | 模板（如「OSS 只读」）、目标子账号（只能选自己的）、用途、时长 | 把子账号加入模板的用户组 | — |
| 云账号权限（按策略）`permission` + 模板 id `policy` | 权限列表里勾选 1–10 条策略、用途、时长（不超过所选策略里最短的上限） | 把策略直接授予本人子账号 | — |
| 访问凭证 `credential` | 模板（对应一个受限角色）、用途、有效期 | 标记为可领取，直到有效期结束 | STS 临时凭证，每次领取现签，最长按模板上限 |

有时长的权限到期后由 `delivery requests sweep` 移出用户组、撤销授予的策略。开通前子账号本来就在的组 / 已直接授予的策略、同一子账号还有别的未到期申请给的，都不收回。
「本来就在」只在第一次开通时判断，且不包括别的申请单加进去的组：到期前续期、开通失败后重试，都不会让限时权限变成永久。

申请页每一项都标出对本人的状态：可申请、已拥有（权限快照里子账号已在全部用户组，可续期）、
申请中、可领取、不可申请（附原因）。「已拥有」只是提示，快照没有或没采全时按「未知」显示为可申请。
模板可选填 `category`（如「存储」「AI 训练」），只用于申请页分类筛选，不影响开通内容，改分类也不影响审批中的申请。

权限列表（`GET /api/policies`）列出本人有子账号的每个云账号里的全部策略，标出：可申请、已有（快照里直接授予
或经用户组继承）、申请中、不开放（附原因），并给出风险和最长天数。按策略申请的单子在开通前除了核对飞书审批，
还要确认每条策略仍在**当前**目录里、没被规则禁用、最长天数没被调低，否则不开通。

申请单详情给申请人和管理员「去飞书查看审批」的链接（飞书 AppLink，PC 与移动端各一个，客户端 7.3.0 起支持）。

## 4. 申请单状态

```
提交 ──► 待审批 ──► 已通过 ──► 执行中 ──► 已完成
          │           │           │
          ├► 已拒绝    │           └► 执行失败 ──(管理员重试)──► 执行中
          └► 已撤回    └► （凭证类）可领取 ──► 已过期
                                  已完成（权限类到期）──► 已到期回收
```

- 状态只能按上图流转，其他转换一律拒绝。
- 「开通中」「提交中」超过 30 分钟没有进展视为中断，标成失败，由管理员核对后重试或关闭。
- 飞书里通过后又被撤销（实例 `reverted`）的审批，一律按未通过处理。
- 「已通过」只由同步飞书审批状态得到；同步可以是员工或管理员打开申请单，或 `delivery requests sweep`
  定时任务——都只是「去飞书查一次」，结论以飞书接口返回为准。

## 5. 组件

```
浏览器 / delivery CLI
        │  （飞书登录或公司 IAM，经 oauth2-proxy）
        ▼
server.py ── HTTP 接口、角色校验、CSRF
   ├─ tickets.py    申请单：状态机、存储、审计
   ├─ catalog.py    模板目录（identity/request-templates.json）
   ├─ approval.py   飞书审批：发起、查询、校验
   ├─ provision.py  执行器：阿里云 / 火山的开号、加组、STS
   ├─ flows.py      申请流程（面板和 CLI 共用）
   ├─ requests_api.py 申请接口与员工视角裁剪
   ├─ assets.py     云账号资产（资源中心）
   ├─ policies.py   权限策略目录（采集）与申请规则
   ├─ views.py      权限页面数据（员工视角 / 管理员视角分开）
   └─ review.py     名册审核
```

数据文件都在 gitignored 的 `identity/`，0600：

| 文件 | 内容 |
|---|---|
| `tickets.json` | 申请单与审计事件 |
| `request-templates.json` | 模板目录（示例：`identity/request-templates.example.json`）|
| `approval.json` | 飞书审批定义编号与表单控件 ID（示例：`identity/approval.example.json`）|
| `assets.json` | 资产快照 |
| `policies.json` | 权限策略目录快照（`delivery policies collect`）|
| `policy-rules.json` | 可选：按策略申请的规则（示例：`identity/policy-rules.example.json`）|

## 6. 开源件怎么用

| 环节 | 用什么 | 为什么不自己写 |
|---|---|---|
| 登录 | oauth2-proxy + 公司 IAM | OIDC 细节多，成熟件更安全 |
| 审批 | 飞书审批 | 审批记录、审批人配置、移动端都现成 |
| CLI 里调云 | 官方 `aliyun` / `ve` CLI | `delivery creds` 只负责给它们提供临时凭证，不重写各云命令 |
| 签名调用 | 仓库内零依赖实现（已有） | 仓库硬规定不引运行时依赖 |

## 7. 现状

| 能力 | 状态 |
|---|---|
| 员工 / 管理后台分开（两套导航、管理操作只在后台） | 已完成 |
| 申请模板、飞书审批发起与核对、状态机、审计事件 | 已完成 |
| 云账号权限申请（加组）、到期自动移出（不误删原有权限） | 已完成 |
| 权限列表与按策略申请（授予 / 到期撤销策略，禁用清单与风险天数） | 已完成 |
| 访问凭证申请（STS 现签现发，不落盘） | 已完成 |
| 开账号（建子账号、加默认组、写人工对应、一次性初始密码） | 已完成 |
| 云账号资产（两家资源中心；员工看数量，管理员看明细） | 已完成 |
| `delivery` CLI：`request` / `creds` / `assets` / `requests sweep` / `approval widgets` | 已完成 |
| 真实云上联调（执行身份、资源中心、飞书审批定义） | 待准备配置后实测 |
| 状态通知：申请人飞书私信（开通、可领取、失败、未通过、即将到期、到期收回），开通失败告警到管理员群（`notify.py`，`DELIVERY_NOTIFY=1`） | 已完成 |
| 申请人以外的审批人同意才开通（`task_list` 核对，`allow_self_approval` 默认关） | 已完成 |
| 管理后台「权限规则」：全部策略是否对员工开放、原因、风险天数（只读） | 已完成 |
| 管理后台「系统状态」：配置缺项、快照过期、执行身份、通知、卡住的申请单（`health.py`，不调外部接口） | 已完成 |
| 申请详情一键打开飞书审批（PC / 手机 AppLink） | 已完成 |
| 公司 IAM 模式下的 CLI 登录（设备码授权 + oauth2-proxy 校验 JWT，`iam_device.py`） | 已完成，待 IT 开启设备码后实测 |
| 飞书审批回调 | 后续（目前靠打开页面同步和定时任务） |

## 8. 代码地图

| 文件 | 职责 |
|---|---|
| `catalog.py` | 模板目录加载与校验 |
| `approval.py` | 飞书审批：发起、查询、撤回、核对 |
| `tickets.py` | 申请单存储、状态机、事件 |
| `flows.py` | 提交、同步、开通、领取、到期回收（面板和 CLI 共用） |
| `provision.py` | 执行器：阿里云 / 火山的写操作 |
| `requests_api.py` | 申请相关 HTTP 接口与员工视角裁剪 |
| `assets.py` | 资源中心采集与视图 |
| `policies.py` | 权限策略目录采集、申请规则（禁用、风险、天数） |
| `notify.py` | 申请状态通知：申请人飞书私信卡片、开通失败管理员告警 |
| `health.py` | 管理后台系统状态检查 |
| `cli_requests.py` | CLI 子命令 |
| `web/core.js` · `web/requests.js` · `web/assets.js` | 前端公共工具、申请页面、资产页面 |
